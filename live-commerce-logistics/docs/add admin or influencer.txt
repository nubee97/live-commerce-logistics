insert into public.user_accounts (
  id,
  login_id,
  password_hash,
  role,
  influencer_name,
  is_active
)
values (
  gen_random_uuid(),
  'User3',
  encode(extensions.digest('inf3', 'sha256'), 'hex'),
  'influencer',
  'User3',
  true
);

insert into public.user_accounts (
  id,
  login_id,
  password_hash,
  role,
  influencer_name,
  is_active
)
values (
  gen_random_uuid(),
  'Admin2',
  encode(extensions.digest('Admin234', 'sha256'), 'hex'),
  'admin',
  '',
  true
);

